<div class="dev-banner">
  <span class="dev-dot" aria-hidden="true"></span>
  <p>Site in development: preview build, not yet intended for public use</p>
</div>

<style>
  .dev-banner {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.6rem;
    background: var(--ink);
    color: rgba(243, 239, 230, 0.78);
    padding: 0.45rem 1rem;
    font-family: 'DM Mono', ui-monospace, monospace;
    font-size: 0.66rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    border-bottom: 1px solid rgba(192, 178, 131, 0.25);
  }
  .dev-banner p { margin: 0; }
  .dev-dot {
    width: 6px;
    height: 6px;
    border-radius: 999px;
    background: var(--soma-gold);
    box-shadow: 0 0 0 0 rgba(192, 178, 131, 0.6);
    animation: devpulse 2.4s ease-in-out infinite;
    flex-shrink: 0;
  }
  @keyframes devpulse {
    0%, 100% { box-shadow: 0 0 0 0 rgba(192, 178, 131, 0.55); }
    50% { box-shadow: 0 0 0 5px rgba(192, 178, 131, 0); }
  }
  @media (max-width: 480px) {
    .dev-banner { font-size: 0.58rem; letter-spacing: 0.12em; }
  }
</style>
